# Публикация обновлённой версии

Этот служебный коммит запускает атомарную проверку и публикацию упрощённого anti-vibecode сайта. После успешного workflow ветка `main` автоматически заменяется чистыми исходниками, а production build публикуется в `gh-pages`.
